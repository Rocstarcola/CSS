<section class="strip">
  <div class="container strip-inner">
    {%- for block in section.blocks -%}
      <div class="strip-item" {{ block.shopify_attributes }}>
        <span class="strip-num">{{ block.settings.title }}</span>
        <span>{{ block.settings.text }}</span>
      </div>
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "Trust strip",
  "max_blocks": 4,
  "blocks": [
    {
      "type": "item",
      "name": "Item",
      "settings": [
        { "type": "text", "id": "title", "label": "Title", "default": "100%" },
        { "type": "text", "id": "text", "label": "Text", "default": "3D-printed in-house" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Trust strip",
      "blocks": [
        { "type": "item", "settings": { "title": "100%", "text": "3D-printed in-house" } },
        { "type": "item", "settings": { "title": "Refillable", "text": "Reusable sturdy boxes" } },
        { "type": "item", "settings": { "title": "Custom", "text": "Branded to your shop" } },
        { "type": "item", "settings": { "title": "Kid-safe", "text": "Age-appropriate prizes" } }
      ]
    }
  ]
}
{% endschema %}
